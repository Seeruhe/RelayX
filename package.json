{
  "name": "proxy-control-monorepo",
  "private": true,
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "test": "cargo test --workspace",
    "lint": "cargo fmt --check && cargo clippy --workspace --all-targets -- -D warnings",
    "test:web": "npm --prefix apps/web run test:contract"
  }
}
