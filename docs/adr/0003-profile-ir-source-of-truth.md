# ADR 0003: Profile IR source of truth

`profile_versions.ir_json` is the desired-state source of truth. Runtime-specific JSON is a compiled artifact and must be reproducible from IR plus secret references and deployment context.
